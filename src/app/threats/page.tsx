"use client";import {useEffect,useState} from "react";import {Search,RefreshCw} from "lucide-react";import Link from "next/link";import {AppShell} from "@/components/layout/AppShell";import {EmptyState,Input,PageTitle,RiskBadge,Panel,Button} from "@/components/ui";import {listThreats} from "@/services/api/threatApi";import type {Threat} from "@/types/api";
export default function Threats(){const [items,setItems]=useState<Threat[]>([]),[q,setQ]=useState(""),[loading,setLoading]=useState(true);async function load(){setLoading(true);try{setItems(await listThreats())}finally{setLoading(false)}}useEffect(()=>{load()},[]);const filtered=items.filter(t=>(t.threat_type+" "+t.source_type+" "+t.severity+" "+t.status).toLowerCase().includes(q.toLowerCase()));return <AppShell><PageTitle title="Threat Management" description="Review AI-detected threats, evidence, model analyses, and status." action={<Button variant="ghost" onClick={load}><RefreshCw className="h-4 w-4"/>Refresh</Button>}/><Panel className="overflow-hidden"><div className="border-b border-white/10 p-4"><div className="relative max-w-md"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-600"/><Input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search threats..." className="pl-10"/></div></div><div className="overflow-x-auto">{loading?<div className="p-10 text-center text-slate-500">Loading threats…</div>:filtered.length?<table className="w-full text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase tracking-wide text-slate-600"><tr><th className="px-5 py-4">Threat</th><th className="px-5 py-4">Source</th><th className="px-5 py-4">Risk</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Detected</th></tr></thead><tbody>{filtered.map(t=><tr key={t.id} className="border-b border-white/5 hover:bg-white/[.025]"><td className="px-5 py-4"><Link href={"/threats/"+t.id} className="font-medium text-cyan-200 hover:text-cyan-100">{t.threat_type.replaceAll("_"," ")}</Link><div className="mt-1 text-xs text-slate-600">#{t.id}</div></td><td className="px-5 py-4 text-slate-400">{t.source_type}</td><td className="px-5 py-4"><div className="flex items-center gap-2"><span className="font-semibold">{t.risk_score}</span><RiskBadge value={t.severity}/></div></td><td className="px-5 py-4 text-slate-400">{t.status.replaceAll("_"," ")}</td><td className="px-5 py-4 text-slate-500">{new Date(t.detected_at).toLocaleString()}</td></tr>)}</tbody></table>:<div className="p-8"><EmptyState text="No matching threats found."/></div>}</div></Panel></AppShell>}
