"use client";import {useState} from "react";import Link from "next/link";import {Shield,ArrowLeft,Eye,EyeOff} from "lucide-react";import {useRouter} from "next/navigation";import {useAuth} from "@/hooks/useAuth";import {Button,Input,Panel} from "@/components/ui";
type Form={first_name:string;last_name:string;email:string;phone_number:string;user_id:string;password:string;password_confirm:string};
const initial:Form={first_name:"",last_name:"",email:"",phone_number:"",user_id:"",password:"",password_confirm:""};
function apiError(error:unknown){if(!(error instanceof Error))return"Registration failed. Please try again.";try{const parsed=JSON.parse(error.message);if(parsed?.detail)return parsed.detail;if(parsed&&typeof parsed==="object")return Object.entries(parsed).map(([k,v])=>`${k}: ${Array.isArray(v)?v.join(", "):String(v)}`).join(" ")}catch{}return error instanceof Error?error.message:"Registration failed. Please try again."}
export default function Register(){const {signUp}=useAuth();const router=useRouter();const [form,setForm]=useState< Form>(initial);const [show,setShow]=useState(false),[showConfirm,setShowConfirm]=useState(false),[err,setErr]=useState(""),[loading,setLoading]=useState(false);const set=(k:keyof Form,v:string)=>setForm(x=>({...x,[k]:v}));async function submit(e:React.FormEvent){e.preventDefault();setErr("");if(form.password.length<12){setErr("Password must contain at least 12 characters.");return}if(form.password!==form.password_confirm){setErr("Passwords do not match.");return}setLoading(true);try{await signUp(form);router.replace("/login?registered=1")}catch(e){setErr(apiError(e))}finally{setLoading(false)}}return <main className="space-bg flex min-h-screen items-center justify-center p-5"><div className="w-full max-w-2xl"><div className="mb-6 flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-cyan-400/10 ring-1 ring-cyan-300/20"><Shield className="h-6 w-6 text-cyan-300"/></div><div><div className="text-xl font-semibold">Create Cyber Guard account</div><div className="text-sm text-slate-500">Register using the backend authentication service.</div></div></div><Panel className="p-7 glow"><form onSubmit={submit} className="grid gap-4 sm:grid-cols-2"><label className="text-sm text-slate-400">First name<Input className="mt-2" value={form.first_name} onChange={e=>set("first_name",e.target.value)} placeholder="First name" required/></label><label className="text-sm text-slate-400">Last name<Input className="mt-2" value={form.last_name} onChange={e=>set("last_name",e.target.value)} placeholder="Last name" required/></label><label className="text-sm text-slate-400">Email<Input className="mt-2" type="email" value={form.email} onChange={e=>set("email",e.target.value)} placeholder="you@example.com" required/></label><label className="text-sm text-slate-400">Phone number<Input className="mt-2" type="tel" value={form.phone_number} onChange={e=>set("phone_number",e.target.value)} placeholder="+919876543210" required/></label><label className="text-sm text-slate-400 sm:col-span-2">User ID<Input className="mt-2" value={form.user_id} onChange={e=>set("user_id",e.target.value)} placeholder="e.g. analyst01" required/><span className="mt-1 block text-xs text-slate-600">4–50 characters: letters, numbers, ., _ and -</span></label><label className="text-sm text-slate-400">Password<div className="relative mt-2"><Input className="pr-11" type={show?"text":"password"} value={form.password} onChange={e=>set("password",e.target.value)} placeholder="Minimum 12 characters" required/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-500">{show?<EyeOff className="h-4 w-4"/>:<Eye className="h-4 w-4" />}</button></div></label><label className="text-sm text-slate-400">Confirm password<div className="relative mt-2"><Input className="pr-11" type={showConfirm?"text":"password"} value={form.password_confirm} onChange={e=>set("password_confirm",e.target.value)} placeholder="Re-enter password" required/><button type="button" onClick={()=>setShowConfirm(!showConfirm)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-500">{showConfirm?<EyeOff className="h-4 w-4"/>:<Eye className="h-4 w-4" />}</button></div></label>{err&&<div className="sm:col-span-2 rounded-xl border border-red-400/20 bg-red-400/10 p-3 text-sm text-red-200">{err}</div>}<div className="sm:col-span-2 flex items-center justify-between gap-3 pt-2"><Link href="/login" className="text-sm text-slate-400 hover:text-white"><ArrowLeft className="mr-1 inline h-4 w-4"/>Back to login</Link><Button type="submit" loading={loading}>{loading?"Creating account...":"Create account"}</Button></div></form></Panel></div></main>