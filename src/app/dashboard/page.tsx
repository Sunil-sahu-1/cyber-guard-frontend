"use client";import {useEffect,useMemo,useState} from "react";import {AlertTriangle,ArrowUpRight,Activity,ShieldCheck,ShieldAlert,Radio} from "lucide-react";import {AppShell} from "@/components/layout/AppShell";import {EmptyState,Panel,PageTitle,RiskBadge} from "@/components/ui";import {listThreats} from "@/services/api/threatApi";import {listIncidents} from "@/services/api/incidentApi";import type {Threat,Incident} from "@/types/api";import {RiskChart} from "@/components/charts/RiskChart";
export default function Dashboard(){const [threats,setThreats]=useState<Threat[]>([]),[incidents,setIncidents]=useState<Incident[]>([]);useEffect(()=>{Promise.all([listThreats(),listIncidents()]).then(([t,i])=>{setThreats(t);setIncidents(i)}).catch(()=>{})},[]);const critical=threats.filter(t=>t.severity==="CRITICAL").length;const high=threats.filter(t=>t.severity==="HIGH").length;const open=incidents.filter(i=>!["RESOLVED","CLOSED"].includes(i.status)).length;const avg=Math.round(threats.length?threats.reduce((a,t)=>a+t.risk_score,0)/threats.length:0);const chart=useMemo(()=>threats.slice(0,7).reverse().map(t=>({label:"#"+t.id,value:t.risk_score})),[threats]);return <AppShell><PageTitle title="Security Command Center" description="Live view of detection activity, risk signals, and active incidents." action={<div className="flex items-center gap-2 text-xs text-emerald-300"><span className="h-2 w-2 rounded-full bg-emerald-400"/>Protected session</div>}/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><Stat icon={ShieldAlert} label="Threats detected" value={threats.length} hint="From your account"/><Stat icon={AlertTriangle} label="High / critical" value={high+critical} hint="Needs attention"/><Stat icon={Radio} label="Open incidents" value={open} hint="Response queue"/><Stat icon={Activity} label="Average risk" value={avg} hint="Out of 100"/></div><div className="mt-5 grid gap-5 xl:grid-cols-[1.6fr_1fr]"><Panel className="p-5"><div className="mb-5 flex items-center justify-between"><div><h2 className="font-semibold">Risk signal history</h2><p className="text-sm text-slate-500">Recent analyzed threats</p></div><ArrowUpRight className="h-5 w-5 text-slate-600"/></div>{chart.length?<RiskChart data={chart}/>:<EmptyState text="Run a scan to populate risk history."/>}</Panel><Panel className="p-5"><h2 className="font-semibold">Recent threats</h2><div className="mt-4 space-y-3">{threats.slice(0,6).map(t=><div key={t.id} className="flex items-center justify-between rounded-xl border border-white/5 bg-white/[.025] p-3"><div className="min-w-0"><div className="truncate text-sm text-slate-200">{t.threat_type.replaceAll("_"," ")} · {t.source_type}</div><div className="mt-1 text-xs text-slate-500">Threat #{t.id}</div></div><div className="flex items-center gap-3"><span className="text-sm font-semibold text-slate-300">{t.risk_score}</span><RiskBadge value={t.severity}/></div></div>)}{!threats.length&&<EmptyState text="No threats recorded yet."/>}</div></Panel></div></AppShell>}
function Stat({icon:Icon,label,value,hint}:{icon:typeof ShieldCheck;label:string;value:number;hint:string}){return <Panel className="p-5"><div className="flex items-center justify-between"><div className="grid h-10 w-10 place-items-center rounded-xl bg-cyan-400/10 text-cyan-300"><Icon className="h-5 w-5"/></div><ShieldCheck className="h-4 w-4 text-emerald-400/70"/></div><div className="mt-5 text-3xl font-semibold">{value}</div><div className="mt-1 text-sm text-slate-300">{label}</div><div className="mt-1 text-xs text-slate-600">{hint}</div></Panel>}