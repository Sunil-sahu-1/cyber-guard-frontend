"use client";import {useEffect,useState} from "react";import {CheckCircle2,RefreshCw} from "lucide-react";import Link from "next/link";import {ProtectedShell} from "@/components/layout/ProtectedShell";import {Button,EmptyState,Panel,PageTitle,RiskBadge} from "@/components/ui";import {listIncidents,resolveIncident} from "@/services/api/incidentApi";import type {Incident} from "@/types/api";
export default function Incidents(){const [items,setItems]=useState<Incident[]>([]);async function load(){try{setItems(await listIncidents())}catch{}}useEffect(()=>{load()},[]);async function resolve(id:number){await resolveIncident(id);load()}return <ProtectedShell><PageTitle title="Incident Response" description="Track high-severity events and the response actions created by the backend." action={<Button variant="ghost" onClick={load}><RefreshCw className="h-4 w-4"/>Refresh</Button>}/><Panel className="overflow-hidden"><div className="overflow-x-auto">{items.length?<table className="w-full text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase tracking-wide text-slate-600"><tr><th className="px-5 py-4">Incident</th><th className="px-5 py-4">Severity</th><th className="px-5 py-4">Risk</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Created</th><th className="px-5 py-4"/></tr></thead><tbody>{items.map(i=><tr key={i.id} className="border-b border-white/5"><td className="px-5 py-4"><Link href={"/incidents/"+i.id} className="font-medium text-cyan-200">{i.title}</Link><div className="mt-1 text-xs text-slate-600">#{i.id} · {i.incident_type}</div></td><td className="px-5 py-4"><RiskBadge value={i.severity}/></td><td className="px-5 py-4 font-semibold">{i.risk_score}</td><td className="px-5 py-4 text-slate-400">{i.status}</td><td className="px-5 py-4 text-slate-500">{new Date(i.created_at).toLocaleString()}</td><td className="px-5 py-4">{!["RESOLVED","CLOSED"].includes(i.status)&&<button onClick={()=>resolve(i.id)} className="inline-flex items-center gap-1 rounded-lg bg-emerald-400/10 px-2.5 py-1.5 text-xs text-emerald-300 ring-1 ring-emerald-300/15"><CheckCircle2 className="h-3.5 w-3.5"/>Resolve</button>}</td></tr>)}</tbody></table>:<div className="p-8"><EmptyState text="No incidents in your queue."/></div>}</div></Panel></ProtectedShell>}